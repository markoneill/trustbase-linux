// a boolean value, if true cipher_list is an exclusive list of allowed ciphers
// if false, cipher list is a list of rejected ciphers
approved_ciphers = false;
// a list of cipher suite identifiers
ciphers_list = (
	0x000a
);

// a list of extention identifiers for required server extentions
required_server_extentions = (
	0xff01
);

// a list of extention identifiers for required server extentions
rejected_server_extentions = (

);

// a list of extention identifiers for required client extentions
required_client_extentions = (
	0x0000
);

// a list of extention identifiers for required client extentions
rejected_client_extentions = (

);
