#include <stdio.h>
#include <string.h>

int query(const char* hostname, unsigned char* certs, size_t certs_length);

int query(const char* hostname, unsigned char* certs, size_t certs_length) {
	return 1;
}
